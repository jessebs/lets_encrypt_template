server {
        server_name *.<%= domain %>;

        index index.php index.html;

        root /var/www/<%= domain %>/www;

        location ~ /.well-known {
                allow all;
        }
}

server {
        server_name <%= domain %>;

        index index.php index.html;

      root /var/www/<%= domain %>/www;

        location ~ /.well-known {
                allow all;
        }
}